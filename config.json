{
    "elasticsearch_url": "https://localhost:9200",
    "elasticsearch_username": "your_username",
    "elasticsearch_password": "your_password",
    "elasticsearch_ca_cert_path": "/path/to/ca.crt",
    "insecure_skip_verify": false,
    "queries_path": "/path/to/queries",
    "prometheus_port": 8000,
    "query_interval": 30, // Globalni interval
    "queries": [
        {
            "name": "query1",
            "type": "raw",
            "query": {
                // Vaš upit ovdje
            },
            "interval": 60 // Poseban interval za query1
        },
        {
            "name": "query2",
            "type": "raw",
            "query": {
                // Vaš drugi upit ovdje
            }
            // Bez posebnog intervala, koristi se globalni interval
        }
    ]
}